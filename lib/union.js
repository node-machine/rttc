/**
 * Module dependencies
 */

var _ = require('lodash');
var buildSchemaIterator = require('./helpers/build-schema-iterator');


/**
 * union()
 *
 * Given two rttc type schemas, return the most specific type schema that
 * would accept the superset of what both type schemas accept normally.
 *
 * @param  {*} typeSchema
 * @return {*}
 */
module.exports = function union (typeSchema) {

  throw new Error('TODO');
};


